name: plinth_example
description: Showcase app for Plinth UI components.
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  plinth_components:
    path: ../packages/plinth_components
  plinth_hooks:
    path: ../packages/plinth_hooks

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
